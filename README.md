# SimpleIDS

Simple IDS and Logging tool 

GUI-based Intrusion Detection System (IDS) for Windows using Python, integrating real-time network traffic analysis, file system monitoring, and security event log inspection.

Uses port scan detection (Scapy), file integrity monitoring (Watchdog), and failed login detection (Windows Event Logs) with a multithreaded Tkinter interface.
